# F6 — Fair integrated behaviour comparison and model readiness

**Status: PREPARED, NOT DISPATCHED.** Outcome `hive-evaluation`. Type: feature.

You are the feature lead, an independent Hermes root session requested as `openai-codex:gpt-5.6-sol`. Read `/Users/operator/Desktop/research/antelligence-app/docs/research/sol-luna-features-20260910/COMMON.md` first. Your controller must supply an ASSIGNMENT with your owned worktree, frozen base/contract commits, evidence root and worker slot. If absent, report that concrete missing assignment; do not write in the canonical checkout.

## User-visible goal

Turn the joined implementation into an auditable comparison that can show cooperation or memory helping, hurting or making no difference.

## Dependencies

C0, F1, F2, F3, F4

## Inspect before planning

- `/Users/operator/Desktop/research/antelligence-app/backend/swarm_core.py` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/backend/research_models.py` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/scripts/analyze_collective_headroom.py` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/docs/research/hive-20260910/LEARNINGS.md` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/docs/research/hive-20260910/graph-review-reconciliation.md` (source locator; execute from your assigned worktree, not this canonical path).

## Proposed exclusive source ownership

- `scripts/evaluate_hive_behaviour.py`
- `tests/test_hive_evaluation.py`
- `docs/research/hive-next/F6-evaluation.md`

Controller confirms these paths at assignment. More than three paths is a feature scope, not a single unattended patch; split into bounded increments. Shared-core or ownership changes require a contract checkpoint, not overlapping edits.

## Acceptance requirements

1. **F6-R1:** First implement a model-free runner using real joined components, all assigned cases and immutable arm/task/output manifests. This increment is not permission for provider calls. Preserve every historical cohort, scoring rule and report byte.
2. **F6-R2:** Freeze a phased comparison rather than an unbounded factorial: no/structured/raw/shuffled/stale memory with per-arm isolation; task-directed/no-extra-sync vs evidence exchange; full-information solo and independent voting; then fixed/random/targeted routing and fixed-step/adaptive timing with message policy controlled.
3. **F6-R3:** Score safe task completion, unsafe attempts/actual violations, invalid outputs, justified/unnecessary abstention and each novelty class separately. Record unique-fact truth and repeated-assertion error separately from agreement. Report all assigned outcomes, not only valid answers.
4. **F6-R4:** Record physical requests, logical/reused steps, input/output tokens, tools, retries, latency and actual cost, including Queen/controller calls. Equal caps are not equal consumption; unknown accounting is unknown. Use paired analysis only for an actually paired design; use R for mathematical/statistical analysis.
5. **F6-R5:** Gate any real-model pilot on frozen combined source/evaluator review, opaque worker-only inputs, actual provider identity and real message-contract development readiness. The earlier160-character format failure must not recur unnoticed. No fallback/retry or sample-size/win-threshold invention.
6. **F6-R6:** Before any pilot, independently reconcile the existing cumulative$5 authority and reservations; do not reset it or silently substitute a provider. If money/auth is uncertain, stop the model phase and deliver the tested harness as code-tested, not as a completed model study.
7. **F6-R7:** Scientific completion requires an admitted disjoint run, immutable raw outputs, independent aggregation and honest null/negative reporting; publish neither a superiority claim nor a production-ready claim from model-free tests. F5 visual presentation joins when available but cannot change scoring.

## Suggested Luna increments

- Model-free comparison/ledger patch.
- Message-readiness and fail-closed provider-adapter patch without making model requests.
- Only after separate admission: bounded pilot and analysis artifact, not bundled into a timed coding child.

## Required behavioural explanation

The same tasks show whether memory, messages or coordinator recovery changed externally checked outcomes, with their real costs and failures visible.

Return requirement→test→artifact evidence, exact commits and a readable before/after trace. Distinguish FEATURE_READY_FOR_INTEGRATION from CODE_TESTED or CHECKPOINTED. Follow COMMON.md for non-author review and parent readback; never merge or deploy this feature yourself.
