# F2 controller NO-GO — two terminal-contract defects

Observed 2026-09-10T23:46:55.948813+00:00; exact candidate `0756ab884b4a9094bb247861a9c22fa4f17a44fd` over accepted C0 `c5f59de5b7359664606a211a5e98ce22ab6291af`.
No independent review has been run. No canonical source integration.

## Verified behaviour retained
All 95 submitted regression tests reproduced. Controller tested 32 generated chain/fork × revision 0/1 cases through actual C0 bytes, composer, independent verifier and reference solver. Paired-world public/Queen bytes match and an independent enumeration found ZERO common safe assignments (not just one cross-used plan failing). A second simultaneous bench reservation at capacity one is rejected without state mutation. Zero-capacity impossibility is independently derived. Full controller gate: **97 passed, 4 failed**, 101 collected; four failures represent the two defects below.

Evidence: `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f2-01/verification.json`, `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f2-01/stdout.txt`, `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f2-01/junit.xml`, and `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f2-01/test_controller_f2.py`. Real behaviour: `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f2-01/pytest/test_controller_real_compositi0/BEHAVIOUR.json`. All twelve return artifact hashes match actual bytes; five source paths match the exact Git candidate. All prior Sol/Luna sessions ended, actual routes verified.

## F2-H1 — Unknown/no-action outcomes cannot join the canonical C0 event stream (HIGH; F2-R4/R6)
`backend/research_hive_verifier.py:225-227` returns `_base_outcome` at ordinal 0 for unknown, whereas public occupies ordinal 0 and every other terminal branch uses len(events)+1. The individual envelope validates, but `[public, *action_records, outcome]` fails actual C0 `validate_event_stream(..., trusted=True)` with `event ordinals must be contiguous and ordered`.
RED: `test_unknown_no_action_outcome_composes_into_trusted_c0_stream`. Preserve unknown task_success=None and evaluator-derived event identity; author the outcome consistently at the correct ordinal. No C0 edit or relaxation.

## F2-H2 — Reference feasibility ignores the action budget (HIGH; F2-R4/R5)
`backend/research_hive_tasks.py:152-179` returns eight reserve/place actions for a four-sample task with max_actions 0, 1 or 7. `backend/research_hive_verifier.py:64-108` cannot derive this budget impossibility and reports unknown/None for no attempts. Those budgets are accepted by project_task, by C0, and by verify_task (the submitted suite already constructs max_actions=1). Each sample independently requires one reserve and one place: a bounded plan cannot exist below that lower bound. This is not malformed external evaluator state or a requested new task family.
RED: three parameter rows of `test_full_information_solver_and_impossibility_honor_action_budget`. Required: reference_solve returns None and no-action verification reports verified impossible with known False for insufficient budget. Positive boundary max_actions=8 already passes; preserve it, successful generated tasks, independent matching/resource/cycle checks, attempt retention and unsafe classification priority. Keep solver and verifier independent; do not call one to certify the other or trust the evaluation flag.

## Scope/process disposition
- Preserve all existing code, history and former traces. This is one bounded F2 terminal-contract repair patch, not a feature restart. Add repository regressions RED-first, then repair, then execute the entire affected matrix plus unchanged controller probes from the owned lane.
- Run-01 disclosed missing observed solver RED (a command labelled solver-red passed before new tests were added). Preserve that fact; do not retroactively relabel or reconstruct historical TDD. Current repairs require actual RED→GREEN. This disclosure is not authority to delete/recreate existing work.
- Twenty-two untracked entries (regular files plus pytest `current` symlinks) under lane-a/scratch/acceptance-repair-green were inventoried in verification.json. They are preserved, excluded from acceptance evidence and must not be edited/deleted/committed. A clean tracked candidate is required; the exact documented untracked residue is a controller-owned exception for continued use of this lane only, not a general dirty-tree allowance.
- Initial controller residue inventory assumed regular files; pytest directory symlinks disproved that assumption. Inspector corrected to record symlink targets without traversing/deleting them. This was a harness error, not a product defect.
- Fresh restricted exact-source Luna review and complete feature return still required AFTER repair and first-hand controller verification. F3/F5 stay dependency-gated. No settings, approval, model-study, integration or production changes.
