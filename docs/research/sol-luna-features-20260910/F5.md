# F5 — Readable, evidence-backed behaviour replay

**Status: PREPARED, NOT DISPATCHED.** Outcome `hive-behaviour-view`. Type: feature.

You are the feature lead, an independent Hermes root session requested as `openai-codex:gpt-5.6-sol`. Read `/Users/operator/Desktop/research/antelligence-app/docs/research/sol-luna-features-20260910/COMMON.md` first. Your controller must supply an ASSIGNMENT with your owned worktree, frozen base/contract commits, evidence root and worker slot. If absent, report that concrete missing assignment; do not write in the canonical checkout.

## User-visible goal

Let the operator see who knew what, what was communicated, why the plan changed, and whether memory helped—without opaque IDs or invented animation.

## Dependencies

C0, F1, F2

## Inspect before planning

- `/Users/operator/Desktop/research/antelligence-app/frontend/src/pages/ResearchWorkbench.tsx` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/frontend/src/lib/research.ts` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/frontend/src/lib/researchTypes.ts` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/backend/research_api.py` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/docs/research/hive-20260910/memory-gate-report.json` (source locator; execute from your assigned worktree, not this canonical path).

## Proposed exclusive source ownership

- `frontend/src/pages/HiveReplay.tsx`
- `frontend/src/lib/hiveReplay.ts`
- `frontend/tests/hiveReplay.test.ts`
- `docs/research/hive-next/F5-replay.md`

Controller confirms these paths at assignment. More than three paths is a feature scope, not a single unattended patch; split into bounded increments. Shared-core or ownership changes require a contract checkpoint, not overlapping edits.

## Acceptance requirements

1. **F5-R1:** Consume the C0 event/output contract and real recorded model-free development traces from accepted features. Show plain names/roles and sources; keep technical IDs in optional details. Label recorded versus live and scripted versus model-generated prominently.
2. **F5-R2:** Show goal/limits, partial views, selected message, source evidence, plan revision, memory acceptance/invalidation, verifier rejection and terminal disposition. Unavailable data remains unknown, not a green card.
3. **F5-R3:** Let a user step through causal events and compare two recorded conditions. Safe abstention and incomplete work must never look like completed task success.
4. **F5-R4:** Handle malformed, incomplete, version-mismatched, reordered and duplicate trace data without laundering false success. Derived UI summaries must be checked against underlying events.
5. **F5-R5:** A controller-owned navigation/API mounting change occurs only after review; do not concurrently edit ResearchWorkbench routing or the existing shared API. First deliver an isolated component/fixture entry using existing frontend patterns.
6. **F5-R6:** Run meaningful frontend tests plus build/lint as appropriate; Sol verifies actual rendered controls and narrow layout in a local browser. No fabricated screenshot or swarm animation. If F3/F4 traces do not yet exist, explicitly mark their display coverage pending rather than pretending those features ran.

## Suggested Luna increments

- Trace adapter/state derivation patch.
- Readable replay component and browser proof.

## Required behavioural explanation

The user can point to the exact evidence that changed an agent’s action, and see the real verifier result rather than a success animation.

Return requirement→test→artifact evidence, exact commits and a readable before/after trace. Distinguish FEATURE_READY_FOR_INTEGRATION from CODE_TESTED or CHECKPOINTED. Follow COMMON.md for non-author review and parent readback; never merge or deploy this feature yourself.
