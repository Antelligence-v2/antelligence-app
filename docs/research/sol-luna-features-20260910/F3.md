# F3 — Bounded evidence exchange and consultation

**Status: PREPARED, NOT DISPATCHED.** Outcome `hive-communication`. Type: feature.

You are the feature lead, an independent Hermes root session requested as `openai-codex:gpt-5.6-sol`. Read `/Users/operator/Desktop/research/antelligence-app/docs/research/sol-luna-features-20260910/COMMON.md` first. Your controller must supply an ASSIGNMENT with your owned worktree, frozen base/contract commits, evidence root and worker slot. If absent, report that concrete missing assignment; do not write in the canonical checkout.

## User-visible goal

Make missing or contradictory evidence trigger limited, attributable consultation rather than indiscriminate broadcasting or automatic consensus.

## Dependencies

C0, F1, F2

## Inspect before planning

- `/Users/operator/Desktop/research/antelligence-app/backend/research_coldroom.py` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/docs/research/hive-20260910/graph-review-reconciliation.md` (source locator; execute from your assigned worktree, not this canonical path).
- `/Users/operator/Desktop/research/antelligence-app/docs/research/hive-20260910/LEARNINGS.md` (source locator; execute from your assigned worktree, not this canonical path).

## Proposed exclusive source ownership

- `backend/research_hive_communication.py`
- `tests/test_research_hive_communication.py`
- `scripts/probe_hive_communication.py`
- `docs/research/hive-next/F3-communication.md`

Controller confirms these paths at assignment. More than three paths is a feature scope, not a single unattended patch; split into bounded increments. Shared-core or ownership changes require a contract checkpoint, not overlapping edits.

## Acceptance requirements

1. **F3-R1:** Implement typed evidence requests/responses with sender, recipient, source/scope/version, lifetime and causal references. Enforce per-run consultations, message sizes, fan-out and step limits; no authority expansion or hidden all-agent context.
2. **F3-R2:** Record transport events and real resource use. Reject/replay-handle duplicates, expired/wrong-scope evidence, unknown recipients and conflicting replies according to the frozen contract. Missing required evidence ends in bounded abstention, not an invented answer.
3. **F3-R3:** Provide explicit controls: task-directed/no-extra-sync, raw broadcast and structured evidence-scoped reconciliation on the same topology. For routing comparisons hold message/merge policy fixed and supply fixed/random/targeted edges with the same allowed envelope.
4. **F3-R4:** Provide a fixed-step same-merge control before claiming adaptive triggering; the paper reports SSVP synchronization at steps2/4. Do not adopt cosine similarity as a factuality or safety gate.
5. **F3-R5:** Exercise stale-source spread, useful minority evidence, equally credible conflict, cycle/retry exhaustion and fresh-evidence recovery after the existing memory gate abstains. The external verifier remains authoritative.
6. **F3-R6:** Run a model-free actual producer→transport→recipient→planner→verifier probe using accepted F1/F2, not only mocked peers. Emit both a successful consultation and a justified abstention with readable causal traces. LLM effectiveness remains unmeasured here.

## Suggested Luna increments

- Transport/budget patch.
- Controlled policy/trace patch.
- Real F1/F2 consultation probe patch.

## Required behavioural explanation

A worker asks the right source for a missing fact; that reply changes the plan, while a plausible conflicting reply cannot silently become truth.

Return requirement→test→artifact evidence, exact commits and a readable before/after trace. Distinguish FEATURE_READY_FOR_INTEGRATION from CODE_TESTED or CHECKPOINTED. Follow COMMON.md for non-author review and parent readback; never merge or deploy this feature yourself.
