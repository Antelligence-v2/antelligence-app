# Antelligence living research ledger

Update the relevant row when evidence changes. Keep **finding → limitation → hypothesis → implementation → observed result → next test** separate. An experiment is not complete merely because its harness passes. Preserve historical reports and append corrections instead of rescoring them.

| Learning / question | Evidence status | Implemented and exercised | Next discriminating test |
|---|---|---|---|
| Independent approaches can beat conversation | Completed small cloud study; independent vote strongest; formatting confound retained | Original report/headroom diagnostic, unchanged | Real message-contract readiness before a fresh cohort; matched controls and all assigned cases |
| Relevant memory is preferable to dumping everything | Source evidence with domain-specific limits; not yet Antelligence LLM evidence | Scoped episode recall and model-free gate; [31 focused tests and fresh-process outcomes](memory-gate-follow-up.md) | Structured vs raw/no/shuffled memory; isolate per-arm histories |
| A version match is not truth | Current external state verifier rejects stale rules even when revision tag matches | Gate regression proves admitted plan can still fail | Provenance-bearing claim conflicts and dependency invalidation |
| Abstention must be executed, not just labelled | First report used a handmade abstention object; follow-up closes this specific gap | Fresh gate process submits/attempts zero actions on scope miss | Bounded consultation for missing evidence, with justified vs unnecessary abstention |
| More synchronization can spread wrong beliefs | [Context Drift paper v1](https://arxiv.org/html/2606.21666v1); travel-specific; improvement over No-Sync not significant; [source review accepted with corrections](graph-review-reconciliation.md) | No graph-routing implementation claimed | Hold topology fixed when changing message policy, then vice versa; retain no-extra-sync/fixed/random controls and same-merge fixed-step comparator |
| A colony should survive Queen replacement | Hypothesis; ordinary process-restart memory retrieval is not Queen succession | No learned/controller replacement implementation yet | Replace coordinator mid-task; measure duplicate/lost actions, safety and outcome |
| Novel tasks must change structure, not just identifiers | Design constraint from reviewed benchmarks | Current coldroom is public development fixtures only | New task compositions, shifted rules and separated held-out families |

## Review-transport learning

A correct source hash does not prove the reviewer could read the source. JSON-escaped code on one long physical line was clipped before the new function; the first FIT was revoked. A fresh plain-file review read the entire unchanged candidate and passed, followed by parent byte/visibility/test checks. Source snapshots now keep real line breaks and a separate hash manifest. Runtime tests and source review are separate evidence.

## C0 controller acceptance correction

The first returned C0 contract passed its 69 focused tests and a non-author static FIT, but the controller's actual evaluator→outcome-wire probe exposed a known-failure encoding defect. `task_success=False` with no accepted transition was rejected like true success; changing it to null would lose the known failure. The controller regression produced **3 failed, 1 passed** on exact candidate `236cf3b2f43f938b416496c4bff490e064f20986`.

The returned demo directly supplied success=True, so it proves serialization of a trusted claim—not an evaluated task success. A Luna repair at `c5f59de5b7359664606a211a5e98ce22ab6291af` now preserves known False without accepted actions. The controller reproduced GREEN on all four formerly tested terminal cases and ran the new real evaluator probe: six accepted actions completed the task, one accepted reserve was safely incomplete, unreserved placement was rejected with zero accepted changes, and an empty plan stayed a known failure. Three malformed revision/action controls were rejected before replay. These observations closed the reproduced code/evidence gaps. The fresh successor Luna review and complete Sol feature return are now accepted: C0 is frozen at `c5f59de5b7359664606a211a5e98ce22ab6291af`. Parent verified all eight complete physical source ranges from actual reviewer tool results, exact Git/source hashes, actual model identities and matching root/controller evaluator outcomes. See the current [controller acceptance](../sol-luna-features-20260910/CONTROLLER.md). This does not claim learned memory or canonical integration; F1 now owns persistence and targeted invalidation.

The unattended root correctly stopped when its PYTHONPATH override required approval. The controller used the normal supervised tool approval, then proved a narrower 78-test invocation with no interpreter path override. No security settings changed. This does not reopen the completed coldroom memory-action gate or claim a problem in its evaluator. No feature integration or experimental rescoring occurred.

Evidence: `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-c0-01/CONTROLLER-NO-GO.md` and adjacent executable regression/observations. Current owner/return: [feature controller](../sol-luna-features-20260910/CONTROLLER.md).

## F1 source-lineage and same-handle verification gaps

Controller reproduced all 116 submitted tests at `1524185ffbccc470cf7adc959b8e8391be8f43b7`. New adversarial acceptance found three failures covering two defects: stable source lineage was conflated with versioned record identity, so r1→r2 changes retained usable stale descendants; metadata validation at construction did not protect an already-open handle from later corruption. Same-version tests and reopened-handle corruption tests alone missed these boundaries. Parent verified real fresh-process recall/targeted invalidation separately; no learned-memory or superiority claim. The original standalone probe failed for a bad positional oracle (both fixtures matched the same query; deterministic ID order is not relevance order). Preserve it as failed evidence, not a code defect. Exact cases/records: `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f1-01/CONTROLLER-NO-GO.md` and `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f1-01/verification.json`. Bounded Sol/Luna repair is active; successor execution, full independent review and complete F1 return remain required.

### F1 partial repair and command-construction correction

H1 source-revision lineage is controller-proven corrected at `abe6b24a019204cd99b7b3f4f83f76f55f909fa2`. The subsequent H2 worker was blocked before pytest because it wrapped a plain test in shell eval. The controller executed an explicit direct command instead (no security changes), proving **121 passed, 2 failed**, both remaining H2 corruption checks. The surviving RED test is preserved in WIP commit `51e815bf16e7690bb4ce8ccf45522cd112b9071b`; neither WIP nor partial green counts as F1 acceptance. Real restart recall/invalidation passed again. Future makers receive fully expanded one-use literal commands instead of constructing dynamic shell bodies. Evidence: `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f1-02/verification.json`; H2-only continuation and final review remain pending.

### F1 repaired candidate and independent-review boundary

Both reproduced defects are corrected at `bf09e21cc1d1e45f41b172dde6ae516cbc038088`. The parent verified Luna's 121/2 RED to 123/0 GREEN, Sol's 123/0 final run, and independently reran 123/0 against the same clean object with actual separate-process recall/invalidation and preserved SQLite rows. Seven raw source snapshots match Git/external bytes and every physical line is readable. The fixed-path Git/tee transport deviation remains disclosed and is accepted only for those verified bytes, not as a security waiver. Fresh capability-restricted exact-source review and full F1 feature return are underway; code correctness evidence is not feature acceptance, model performance or canonical integration. Evidence: `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f1-03/controller-gate.json` and `/Volumes/WD_BLACK/antelligence-collective-20260908/sol-luna-features-20260910/controller-20260910_154850_489cf7/check-f1-03/SNAPSHOT-ADJUDICATION.md`.

## Current boundary

Model-free substrate development continues. Learned memory, adaptive Queen routing and collective performance improvement remain unproven. A new model experiment requires an admitted task/evaluator/message contract, frozen conditions, real resource accounting and the existing cumulative cost authority. No new scheduling, service ownership, clinical/financial permission or publication is implied.

Source reconciliation: [hive reading](review-reconciliation.md) and [graph-paper reading](graph-review-reconciliation.md). The original [worker critique](graph-paper-critique.md) is preserved alongside parent corrections and the [source-identity receipt](graph-review-acceptance.json); Vault notes and SSD source bytes remain linked. Read the actual algorithm, not only the social post or abstract.
